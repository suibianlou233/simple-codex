# 最新源码交接包（2026-09-15）

## 内容

这是开发机当前工作区快照，包含所有已跟踪文件的最新内容及未提交的新源码/测试/文档，不是旧的 Git 提交导出。
包含匹配的 Windows x64 Codex 内核三个可执行文件、许可证、补丁和校验清单。无需重新编译 Codex 内核即可继续开发 Simple。
不包含 Git 历史、依赖缓存、Simple 编译产物、账户密钥、聊天记录、用户数据库和个人技能。新机器须自行配置模型密钥。
Git 基准提交：c288a9fb493d5e9ae34756382d7acd0627b4fb3e
原始仓库：https://github.com/suibianlou233/simple-codex.git
此目录可直接开发；若需要历史，可另行克隆原仓库后将本包源码覆盖进去。不要把内核二进制提交到 Git。

## 已包含的最新改动

- 长文本粘贴卡片，粘贴后仍可打字；发送后折叠展示，右侧打开正文，保持文字气泡颜色。
- 对话归档/删除与删除速度修复。
- 千问 Token Plan 配置、developer 角色兼容、持续响应误超时修复。
- 千问无 phase 流式消息显示修复。
- 千问 qwen3.8-max / qwen3.8-flash 图片能力、上传图片与工具图片转换、历史图片保留。

## 在 Windows 上继续开发

准备 Git、Windows C++ Build Tools/SDK、WebView2、rust-toolchain.toml 指定的 Rust、Node 20.19+ 或 22.12+、pnpm。
解压后在 simple-codex 目录打开 PowerShell。关闭已运行的 Simple，再执行：

```powershell
./scripts/sync-simple-sandbox-runtime.ps1 -Profile debug
Push-Location apps/desktop
pnpm install --frozen-lockfile
Pop-Location
$env:SIMPLE_CODEX_KERNEL_MANIFEST = (Resolve-Path './kernels/packages/official-283-windows-candidate-1/kernel.json').Path
Push-Location apps/desktop
pnpm tauri dev
Pop-Location
```

这会启动原生桌面开发窗口。Simple 自己的 Rust 后端和辅助程序仍需要构建；随包 Codex 内核可直接复用。
随包内核仅适用于 Windows x64。Mac 需要另行准备匹配平台的内核及开发环境。
正式安装包构建和上游固定版本说明见 docs/DEVELOPMENT.md。

## 验证状态

最新图片修复已通过：Qwen 相关单元测试、7 项协议转换测试、3 项桌面图片附件测试，以及真实固定内核连接模拟千问服务的双轮图片集成测试；前端和原生桌面构建成功。
集成测试验证图片实际进入接口请求及后续历史；没有调用用户付费接口验证图片识别质量。
此前已完成流式显示等验证，详见随包测试及 docs/MODEL-CONNECTIONS.md、docs/LONG-TEXT.md、docs/CONVERSATION-MANAGEMENT.md。
未在全新机器上验证完整开发环境安装流程。

## 文件完整性

HANDOFF-SHA256.json 记录源码及内核每个文件的 SHA-256；压缩包旁的 .sha256 文件用于校验整个 ZIP。
