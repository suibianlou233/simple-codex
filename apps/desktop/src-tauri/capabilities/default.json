{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base desktop capability with no network or filesystem plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
